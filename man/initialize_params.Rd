% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_params.R
\name{initialize_params}
\alias{initialize_params}
\title{Initialize Parameters for the Fish Population Simulation}
\usage{
initialize_params(
  species = NULL,
  nyear = NULL,
  rec.age = NULL,
  first.year = NULL,
  nage = NULL,
  M = NULL,
  init_Z = NULL,
  vbk = NULL,
  Linf = NULL,
  t0 = NULL,
  len_mid = NULL,
  len_border = NULL,
  a = NULL,
  b = NULL,
  mat_L50 = NULL,
  mat_L95 = NULL,
  cv_L = NULL,
  cv_inc = NULL,
  std_logR = NULL,
  std_logN0 = NULL,
  alpha = NULL,
  beta = NULL,
  std_SN = NULL,
  q_surv_L50 = NULL,
  q_surv_L95 = NULL,
  R_init = NULL,
  F_mean = NULL,
  F_ar = NULL,
  F_sd = NULL,
  R_ar = NULL
)
}
\arguments{
\item{species}{Character or NULL. Species preset name, one of
\code{"flatfish"}, \code{"tuna"}, \code{"krill"}, or \code{"?"} to list
presets. NULL (default) uses generic defaults equivalent to flatfish.}

\item{nyear}{Integer. Total simulation years including burn-in (default 100).}

\item{rec.age}{Numeric. Age of recruitment (default 1; use 0.25 for quarterly).}

\item{first.year}{Integer. First year of the observation window (default 2020).}

\item{nage}{Integer. Number of age classes including plus-group (default 15).}

\item{M}{Numeric. Natural mortality rate (default 0.2).}

\item{init_Z}{Numeric. Initial total mortality for equilibrium (default 0.5).}

\item{vbk}{Numeric. Von Bertalanffy k (default 0.2).}

\item{Linf}{Numeric. Asymptotic length (default 60).}

\item{t0}{Numeric. Von Bertalanffy t0 (default 1/60).}

\item{len_mid}{Numeric vector. Midpoints of length bins.}

\item{len_border}{Numeric vector. Borders of length bins.}

\item{a}{Numeric. Length-weight coefficient (default exp(-12)).}

\item{b}{Numeric. Length-weight exponent (default 3).}

\item{mat_L50}{Numeric. Length at 50 pct maturity (default 35).}

\item{mat_L95}{Numeric. Length at 95 pct maturity (default 40).}

\item{cv_L}{Numeric. CV of length-at-age (default 0.2).}

\item{cv_inc}{Numeric. CV of growth increment (default 0.2).}

\item{std_logR}{Numeric. SD of log-recruitment deviations (default 0.3).}

\item{std_logN0}{Numeric. SD of initial log-N deviations (default 0.2).}

\item{alpha}{Numeric. Beverton-Holt alpha (default 400).}

\item{beta}{Numeric. Beverton-Holt beta (default 10).}

\item{std_SN}{Numeric. Survey observation error SD (default 0.2).}

\item{q_surv_L50}{Numeric. Survey selectivity L50 (default 15).}

\item{q_surv_L95}{Numeric. Survey selectivity L95 (default 20).}

\item{R_init}{Numeric. Initial recruitment (default 500).}

\item{F_mean}{Numeric. Mean fishing mortality (default 0.3).}

\item{F_ar}{Numeric. AR1 coefficient for F deviations (default 0.75).}

\item{F_sd}{Numeric. SD of F deviations (default 0.2).}

\item{R_ar}{Numeric. AR1 coefficient for recruitment deviations (default 0.1).}
}
\value{
A list containing all initialized parameters and derived variables
(ages, years, etc.), ready to pass to \code{sim_cal()}.
}
\description{
Initializes all biological, fishing, and survey parameters needed for the
population simulation via \code{sim_cal()} and \code{sim_data()}. Users can
select a built-in species preset (\code{"flatfish"}, \code{"tuna"}, or
\code{"krill"}) to get biologically consistent parameter sets, or specify
all parameters manually.
}
\details{
Species presets and their key parameters:
\itemize{
\item flatfish: nage=15, Linf=60, vbk=0.2, M=0.2, annual (M7 model)
\item tuna: nage=20, Linf=152, vbk=0.38, M=0.2, quarterly (M6 model)
\item krill: nage=5, Linf=60, vbk=0.3, M=0.3, annual
}
}
\examples{
# List available presets
initialize_params(species = "?")

# M7 flatfish (same as default)
params <- initialize_params(species = "flatfish")

# M6 tuna with quarterly time steps
params <- initialize_params(species = "tuna")

# Krill with custom M
params <- initialize_params(species = "krill", M = 0.4)

# Fully custom (no preset)
params <- initialize_params(nage = 10, Linf = 80, vbk = 0.15)

}
