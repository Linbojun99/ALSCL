% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_deviacne.R, R/plot_deviance.R
\name{plot_deviance}
\alias{plot_deviance}
\title{Plot Deviation of Recruitment (R) and Fishing Mortality (F) Over Years}
\usage{
plot_deviance(
  model_result,
  se = TRUE,
  point_size = 3,
  point_color = "white",
  point_shape = 21,
  line_size = 1,
  line_color = "black",
  line_type = "solid",
  se_color = "black",
  se_width = 0.5,
  facet_ncol = NULL,
  facet_scales = "free",
  log = TRUE,
  type = c("R", "F")
)

plot_deviance(
  model_result,
  se = TRUE,
  point_size = 3,
  point_color = "white",
  point_shape = 21,
  line_size = 1,
  line_color = "black",
  line_type = "solid",
  se_color = "black",
  se_width = 0.5,
  facet_ncol = NULL,
  facet_scales = "free",
  log = TRUE,
  type = c("R", "F")
)
}
\arguments{
\item{model_result}{A list from \code{run_acl} or \code{run_alscl}.}

\item{se}{Logical, whether to draw the standard error bar.}

\item{point_size}{Numeric. The size of the point. Default is 3.}

\item{point_color}{Character. The color of the point. Default is "white".}

\item{point_shape}{Numeric. The shape of the point. Default is 21.}

\item{line_size}{Numeric, the line size.}

\item{line_color}{Character, the line color.}

\item{line_type}{Character, the line type.}

\item{se_color}{Character, the color of the standard error.}

\item{se_width}{Numeric, the width of the standard error.}

\item{facet_ncol}{Number of columns in facet wrap. Default is NULL.}

\item{facet_scales}{Scales for facet wrap. Default is "free".}

\item{log}{Logical, whether to keep log scale (TRUE) or apply exp transform (FALSE).}

\item{type}{Character. "R" for recruitment deviation, "F" for fishing mortality deviation.}
}
\value{
A ggplot2 object.

A ggplot2 object.
}
\description{
This function calculates the deviations of R and F in the ACL or ALSCL model results.
For ACL, F deviations are at the age level (A x Y).
For ALSCL, F deviations are at the length level (L x Y).

This function calculates the deviations of R and F in the ACL or ALSCL model results.
For ACL, F deviations are at the age level (A x Y).
For ALSCL, F deviations are at the length level (L x Y).
}
