% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_compare.R
\name{plot_compare_ts}
\alias{plot_compare_ts}
\title{Compare Time-Series Outputs of Two Models}
\usage{
plot_compare_ts(
  model1,
  model2,
  quantities = c("SSB", "R", "B", "N", "CN", "CB"),
  se = FALSE,
  model1_name = NULL,
  model2_name = NULL,
  colors = NULL,
  linetypes = NULL,
  linewidth = NULL,
  ncol = NULL,
  scales = NULL,
  return_data = FALSE
)
}
\arguments{
\item{model1}{First model result (reference).}

\item{model2}{Second model result (alternative).}

\item{quantities}{Character vector. Quantities to compare. Accepts: "SSB", "R" (or "Rec"),
"B", "N", "CN", "CB", "F". Default c("SSB","R","B","N","CN","CB").}

\item{se}{Logical. Show confidence intervals. Default FALSE.}

\item{model1_name, model2_name}{Character. Labels. Auto-detected if NULL.}

\item{colors}{Named character vector of 2 colors. NULL = use global theme.}

\item{linetypes}{Named character vector of 2 linetypes. NULL = use global theme.}

\item{linewidth}{Numeric. NULL = use global theme.}

\item{ncol}{Integer. Facet columns. NULL = use global theme.}

\item{scales}{Character. Facet scales. NULL = use global theme.}

\item{return_data}{Logical. Return data alongside plot. Default FALSE.}
}
\value{
A ggplot object (can be further modified with + theme(), + labs(), etc.)
}
\description{
Plots SSB, Recruitment, Biomass, Abundance, Catch from two models
on the same axes. Fully supports the global theme system and can be
extended with \code{+ ggplot2::theme()} or any ggplot2 layer.
}
\examples{
\dontrun{
# Basic usage
plot_compare_ts(result_acl, result_alscl)

# With CI
plot_compare_ts(result_acl, result_alscl, se = TRUE)

# Per-call override + ggplot2 layering
plot_compare_ts(result_acl, result_alscl,
  colors = c(ACL = "darkgreen", ALSCL = "orange")) +
  ggplot2::theme(legend.position = "top") +
  ggplot2::labs(title = "My Title")

# Or set globally once
acl_theme_set(
  compare_colors = c(ACL = "steelblue", ALSCL = "tomato"),
  compare_linetypes = c(ACL = "solid", ALSCL = "longdash"),
  base_theme = "theme_classic"
)
plot_compare_ts(result_acl, result_alscl)  # uses global settings
}
}
