% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_compare.R
\name{plot_compare_growth}
\alias{plot_compare_growth}
\title{Compare Growth Curves and Length-at-Age Matrices}
\usage{
plot_compare_growth(
  model1,
  model2,
  age_range = c(1, 20),
  model1_name = NULL,
  model2_name = NULL,
  colors = NULL,
  linetypes = NULL,
  linewidth = NULL,
  ref_data = NULL,
  ref_name = "Observed VB",
  ref_color = "grey30",
  ref_linetype = "dotdash",
  show_points = TRUE,
  point_size = NULL,
  point_alpha = 0.4,
  nls_start = NULL
)
}
\arguments{
\item{model1}{First model result (reference).}

\item{model2}{Second model result (alternative).}

\item{age_range}{Numeric vector of length 2. Default c(1, 20).}

\item{model1_name, model2_name}{Character. Labels. Auto-detected if NULL.}

\item{colors}{Named character vector of 2 colors. NULL = use global theme.}

\item{linetypes}{Named character vector of 2 linetypes. NULL = use global theme.}

\item{linewidth}{Numeric. NULL = use global theme.}

\item{ref_data}{Optional data.frame with columns \code{Age} and \code{Length}
containing observed age-length measurements (e.g. from otolith readings).
If provided, a VB curve is fitted via \code{nls()} and overlaid as a
reference, with the raw data as scatter points.}

\item{ref_name}{Character. Label for the reference curve. Default "Observed VB".}

\item{ref_color}{Character. Color for reference curve/points. Default "grey30".}

\item{ref_linetype}{Character. Linetype for reference curve. Default "dotdash".}

\item{show_points}{Logical. Show raw data points from \code{ref_data}. Default TRUE.}

\item{point_size}{Numeric. Size of data points. NULL = use global theme.}

\item{point_alpha}{Numeric. Transparency of data points. Default 0.4.}

\item{nls_start}{Named list. Starting values for nls. Default attempts
auto-detection from data range.}
}
\value{
A ggplot object.
}
\description{
Left panel: VB growth curves from each model. Optionally overlay a VB curve
fitted independently from observed age-length data via \code{nls()}.
Right panel: pla heatmaps side-by-side.
}
\examples{
\dontrun{
# Without reference data
plot_compare_growth(result_acl, result_alscl)

# With observed age-length data
obs <- data.frame(Age = c(1,1,2,2,3,3,4,5,6,7),
                  Length = c(20,22,30,28,38,35,42,48,50,52))
plot_compare_growth(result_acl, result_alscl, ref_data = obs)

# Customize reference
plot_compare_growth(result_acl, result_alscl,
  ref_data = obs, ref_name = "Otolith data",
  ref_color = "forestgreen", show_points = TRUE)
}
}
